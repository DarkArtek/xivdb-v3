function randomXToY(minVal,maxVal)
{
    return Math.floor(Math.random()*maxVal)+minVal
}
function ramdomXToYFloat(minVal,maxVal)
{
    return (Math.random() * (maxVal - minVal) + minVal).toFixed(4)
}
